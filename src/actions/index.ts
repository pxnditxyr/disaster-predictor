import { createDisasterType, deleteDisasterType, getDisasterType, updateDisasterType } from './disasters'

export const server = {
  getDisasterType,
  createDisasterType,
  updateDisasterType,
  deleteDisasterType,
}
