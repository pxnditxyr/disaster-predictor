---
import Layout from '@/modules/shared/layouts/Layout.astro'
import Title from '@/modules/shared/components/ui/Title.astro'
import HomeNavbar from '@/modules/home/components/ui/HomeNavbar.astro'
import Footer from '@/modules/shared/components/ui/Footer.astro'
import { Icon } from 'astro-icon/components'
import { LastMitigations } from '@/modules/home/components/info/LastMitigations.tsx'
import { AllMitigations } from '@/modules/home/components/info/AllMitigations.tsx'

import { mitigationActions } from '@/constants/mitigations-data'

---

<Layout>
  <div class="min-h-screen w-full">
    <HomeNavbar />

    <Title class="text-center text-pretty mt-16"> 🛡️ Mitigaciones 🏥 </Title>


    <div class="w-full flex flex-col justify-center items-center mt-16 gap-12 lg:flex-row lg:gap-4 px-4 sm:px-12 lg:mt-32">
      <div class="w-full px-4 lg:w-1/3 lg:px-0 flex items-center justify-center">
        <Icon name="luggage-insurance" size={ 200 } />
      </div>
      <div class="w-full px-4 lg:w-2/3 lg:px-0">
        <LastMitigations client:only="react" />
      </div>
    </div>
  </div>

  <div class="min-h-screen w-full">
    <Title class="text-center text-pretty my-8"> 📜 Historial de Mitigaciones 🏥 </Title>
    <div class="w-full px-4 flex items-center justify-center">
      <p class="text-pretty text-[#5c4f3d] text-md max-w-[600px]">
        En esta sección se muestra el historial de mitigaciones realizadas en el sistema.
        La tabla muestra las mitigaciones desde las mas recientes hasta las mas antiguas.
      </p>
    </div>
    <div class="w-full flex flex-col justify-center items-center mt-16 gap-12 lg:flex-row lg:gap-4 px-4 sm:px-12">
      <div class="w-full px-4 lg:w-2/3 lg:px-0">
        <AllMitigations client:only="react" />
      </div>
    </div>

  </div>


  <div class="pb-20">
    <Title class="text-center text-pretty my-12"> 🛡️ Acciones de Mitigación 🏥 </Title>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 py-12 px-8">
      {
        Object.entries( mitigationActions ).map( ([ disaster, data ]) => (
          <div class="bg-[#faf3e0] border-[#d2b48c] p-4 rounded-xl shadow-lg shadow-[#f0e6d2] border-2">
            <div class="flex flex-row items-center space-x-4 pb-2">
              <Icon name={ data.icon } size={ 60 } />
              <div class="text-xl font-semibold text-[#8B4513]">{disaster}</div>
            </div>
            <div>
              <ul class="list-disc pl-5 space-y-2">
                {
                  data.actions.map( ( action ) => (
                    <li class="text-sm text-[#5c4f3d]">
                      { action }
                    </li>
                  )  )
                }
              </ul>
            </div>
          </div>
        ) )
      }
    </div>
  </div>

  <div class="w-full mt-36">
    <Footer />
  </div>
</Layout>
